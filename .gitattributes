# 统一换行：仓库内一律 LF，避免 .sh 脚本带 CRLF 在 Linux/NAS 上执行报 \r 错误
* text=auto eol=lf

# 脚本必须 LF（在飞牛/群晖等 Linux 环境直接执行）
*.sh text eol=lf
*.py text eol=lf

# 二进制不参与换行转换
*.png binary
*.jpg binary
*.ico binary
*.pyc binary
